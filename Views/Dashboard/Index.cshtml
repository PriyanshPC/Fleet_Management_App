@{
    ViewBag.Title = "Dashboard";
}
<div class="app-card p-3 d-flex align-items-center justify-content-between">
    <div>
        <h2 class="h5 mb-1">Dashboard</h2>
        <p class="text-muted mb-0">We’ll add graphs and active rentals next.</p>
    </div>
    <a href="@Url.Action("New", "Rentals")" class="btn btn-outline-light">
        <i class="bi bi-plus-lg me-1"></i> New Rental
    </a>
</div>
